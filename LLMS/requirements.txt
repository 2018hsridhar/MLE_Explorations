# Core LLM & OpenAI Libraries
langchain>=0.1.0
langchain_openai>=0.1.0
openai>=1.0.0

# Development Tools
git-filter-repo

# Upgrade commands are not needed in requirements.txt
# To upgrade, use pip install --upgrade <package> in the terminal